# Important
There are hidden files in this directory. DO NOT DELETE!

# Instructions
1. Clone dotfiles repo: `git clone https://github.com/hyliancoder/dotfiles.git` into `~`
2. Run `./install-dotfiles`

*To debug, use `bash -x ./install-dotfiles`*
