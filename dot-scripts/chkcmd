#!/bin/bash
set -euo pipefail

check_command() {
    for cmd in "${@}"; do
        if ! command -v "${cmd}" 1>/dev/null; then
            catch -i "Command '${cmd}' not found"
            status=1
        fi
    done
    return "${status:-0}"
}

check_command "${@}"